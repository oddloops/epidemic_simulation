#pragma once
#ifndef UTIL
#define UTIL

#include <string>
#include <vector>
#include "Enums.h"

std::string healthToString(HealthStatus status);
std::string infectionToString(InfectionStatus status);

#endif